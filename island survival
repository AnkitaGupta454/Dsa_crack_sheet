#include<bits/stdc++.h>
using namespace std;

int main()
{
    int S=10,N=16,M=2;
    if(((N*6)<(M*7))&&((S>6)||(M>N)))
    cout<<"No\n";
    else
    {
        int days=(M*S)/N;
        if((M*S)%N!=0)
        days++;
        cout<<"yes "<<days;

    }
    return 0;
}

output:-
yes 2
